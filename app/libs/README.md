# Optional Local AARs

This folder is intentionally empty in the public repository.

The current `v2.1` app does not require any proprietary vendor SDK to compile.

If someone experiments with vendor-specific integrations later, local `.aar` or `.jar`
files can be dropped here and loaded through Gradle without committing them.

Those files should stay local unless their license explicitly allows redistribution.
